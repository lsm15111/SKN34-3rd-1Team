<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ai.govbiz.core.account.repository.mapper.AccountMapper">

    <resultMap id="accountDbRow"
               type="ai.govbiz.core.account.repository.mapper.AccountDbRow">
        <id column="id" property="id" />
        <result column="email" property="email" />
        <result column="password_hash" property="passwordHash" />
        <result column="role" property="role" />
        <result column="email_verified_at" property="emailVerifiedAt" />
        <result column="suspended_at" property="suspendedAt" />
        <result column="deleted_at" property="deletedAt" />
        <result column="terms_agreed_at" property="termsAgreedAt" />
        <result column="created_at" property="createdAt" />
        <result column="company_id" property="companyId" />
        <result column="company_name" property="companyName" />
        <result column="company_business_number" property="companyBusinessNumber" />
    </resultMap>

    <resultMap id="accountSocialIdentityDbRow"
               type="ai.govbiz.core.account.repository.mapper.AccountSocialIdentityDbRow">
        <id column="id" property="id" />
        <result column="account_id" property="accountId" />
        <result column="provider" property="provider" />
        <result column="provider_user_id" property="providerUserId" />
        <result column="email" property="email" />
        <result column="linked_at" property="linkedAt" />
    </resultMap>

    <resultMap id="accountSessionDbRow"
               type="ai.govbiz.core.account.repository.mapper.AccountSessionDbRow">
        <id column="id" property="id" />
        <result column="token_hash" property="tokenHash" />
        <result column="account_id" property="accountId" />
        <result column="last_used_at" property="lastUsedAt" />
        <result column="expires_at" property="expiresAt" />
    </resultMap>

    <sql id="accountColumns">
        account.id,
        account.email,
        account.password_hash,
        account.role,
        account.email_verified_at,
        account.suspended_at,
        account.deleted_at,
        account.terms_agreed_at,
        account.created_at,
        company.id AS company_id,
        company.company_name AS company_name,
        company.business_number AS company_business_number
    </sql>

    <insert id="insertAccount"
            parameterType="ai.govbiz.core.account.repository.mapper.AccountDbRow"
            useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO account (
            email,
            password_hash,
            role,
            email_verified_at,
            terms_agreed_at,
            created_at
        ) VALUES (
            #{email},
            #{passwordHash},
            #{role},
            #{emailVerifiedAt},
            #{termsAgreedAt},
            #{createdAt}
        )
    </insert>

    <select id="findAccountById" resultMap="accountDbRow">
        SELECT
            <include refid="accountColumns" />
        FROM account
        LEFT JOIN company ON company.account_id = account.id
        WHERE account.id = #{id}
          AND account.deleted_at IS NULL
    </select>

    <select id="findAccountByEmail" resultMap="accountDbRow">
        SELECT
            <include refid="accountColumns" />
        FROM account
        LEFT JOIN company ON company.account_id = account.id
        WHERE account.email = #{email}
          AND account.deleted_at IS NULL
    </select>


    <insert id="insertSession"
            parameterType="ai.govbiz.core.account.repository.mapper.AccountSessionDbRow"
            useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO account_session (
            token_hash,
            account_id,
            last_used_at,
            expires_at
        ) VALUES (
            #{tokenHash},
            #{accountId},
            #{lastUsedAt},
            #{expiresAt}
        )
    </insert>

    <select id="findSessionByTokenHash" resultMap="accountSessionDbRow">
        SELECT
            session.id,
            session.token_hash,
            session.account_id,
            session.last_used_at,
            session.expires_at
        FROM account_session session
        WHERE session.token_hash = #{tokenHash}
    </select>

    <update id="updateSessionLastUsedAt">
        UPDATE account_session
        SET last_used_at = #{lastUsedAt}
        WHERE token_hash = #{tokenHash}
    </update>

    <delete id="deleteSessionByTokenHash">
        DELETE FROM account_session
        WHERE token_hash = #{tokenHash}
    </delete>

    <delete id="deleteExpiredSessionsByAccountId">
        DELETE FROM account_session
        WHERE account_id = #{accountId}
          AND expires_at &lt;= #{now}
    </delete>

    <update id="updateEmailVerifiedAtIfNull">
        UPDATE account
        SET email_verified_at = #{verifiedAt}
        WHERE id = #{accountId}
          AND email_verified_at IS NULL
    </update>

    <select id="findSocialIdentity" resultMap="accountSocialIdentityDbRow">
        SELECT
            identity.id,
            identity.account_id,
            identity.provider,
            identity.provider_user_id,
            identity.email,
            identity.linked_at
        FROM account_social_identity identity
        JOIN account ON account.id = identity.account_id
        WHERE identity.provider = #{provider}
          AND identity.provider_user_id = #{providerUserId}
          AND account.deleted_at IS NULL
    </select>

    <insert id="insertSocialIdentity"
            parameterType="ai.govbiz.core.account.repository.mapper.AccountSocialIdentityDbRow"
            useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO account_social_identity (
            account_id,
            provider,
            provider_user_id,
            email,
            linked_at
        ) VALUES (
            #{accountId},
            #{provider},
            #{providerUserId},
            #{email},
            #{linkedAt}
        )
    </insert>
</mapper>
