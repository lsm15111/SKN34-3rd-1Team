<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ai.govbiz.core.supportprogram.repository.mapper.SupportProgramMapper">

    <resultMap id="supportProgramDbRow"
               type="ai.govbiz.core.supportprogram.repository.mapper.SupportProgramDbRow">
        <id column="source_code" property="sourceCode" />
        <id column="source_program_id" property="sourceProgramId" />
        <result column="title" property="title" />
        <result column="organization" property="organization" />
        <result column="summary" property="summary" />
        <result column="categories" property="categoriesJson" />
        <result column="regions" property="regionsJson" />
        <result column="target_description" property="targetDescription" />
        <result column="application_period_raw" property="applicationPeriodRaw" />
        <result column="application_start_date" property="applicationStartDate" />
        <result column="application_end_date" property="applicationEndDate" />
        <result column="source_url" property="sourceUrl" />
        <result column="source_sort_timestamp" property="sourceSortTimestamp" />
        <result column="startup_details" property="startupDetailsJson" />
    </resultMap>

    <resultMap id="supportProgramSourceDocumentDbRow"
               type="ai.govbiz.core.supportprogram.repository.mapper.SupportProgramSourceDocumentDbRow">
        <id column="source_code" property="sourceCode" />
        <id column="source_program_id" property="sourceProgramId" />
        <result column="source_url" property="sourceUrl" />
        <result column="content" property="content" />
        <result column="content_hash" property="contentHash" />
        <result column="fetched_at" property="fetchedAt" />
    </resultMap>

    <resultMap id="supportProgramSyncStatusDbRow"
               type="ai.govbiz.core.supportprogram.repository.mapper.SupportProgramSyncStatusDbRow">
        <id column="source_code" property="sourceCode" />
        <result column="published_generation" property="publishedGeneration" />
        <result column="published_catalog_fingerprint" property="publishedCatalogFingerprint" />
        <result column="published_program_count" property="publishedProgramCount" />
        <result column="index_ready" property="indexReady" />
        <result column="last_successful_sync_at" property="lastSuccessfulSyncAt" />
        <result column="last_failed_sync_at" property="lastFailedSyncAt" />
        <result column="last_sync_outcome" property="lastSyncOutcome" />
    </resultMap>

    <insert id="upsert"
            parameterType="ai.govbiz.core.supportprogram.repository.mapper.SupportProgramDbRow">
        INSERT INTO support_program (
            source_code,
            source_program_id,
            title,
            organization,
            summary,
            categories,
            regions,
            target_description,
            application_period_raw,
            application_start_date,
            application_end_date,
            source_url,
            source_sort_timestamp,
            startup_details,
            is_source_present
        ) VALUES (
            #{sourceCode},
            #{sourceProgramId},
            #{title},
            #{organization},
            #{summary},
            #{categoriesJson,jdbcType=VARCHAR},
            #{regionsJson,jdbcType=VARCHAR},
            #{targetDescription},
            #{applicationPeriodRaw},
            #{applicationStartDate,jdbcType=DATE},
            #{applicationEndDate,jdbcType=DATE},
            #{sourceUrl},
            #{sourceSortTimestamp,jdbcType=VARCHAR},
            #{startupDetailsJson,jdbcType=VARCHAR},
            TRUE
        )
        ON DUPLICATE KEY UPDATE
            source_program_id = #{sourceProgramId},
            title = #{title},
            organization = #{organization},
            summary = #{summary},
            categories = #{categoriesJson,jdbcType=VARCHAR},
            regions = #{regionsJson,jdbcType=VARCHAR},
            target_description = #{targetDescription},
            application_period_raw = #{applicationPeriodRaw},
            application_start_date = #{applicationStartDate,jdbcType=DATE},
            application_end_date = #{applicationEndDate,jdbcType=DATE},
            source_url = #{sourceUrl},
            source_sort_timestamp = #{sourceSortTimestamp,jdbcType=VARCHAR},
            startup_details = #{startupDetailsJson,jdbcType=VARCHAR},
            is_source_present = TRUE,
            last_seen_at = CURRENT_TIMESTAMP(6)
    </insert>

    <insert id="upsertBatch">
        INSERT INTO support_program (
            source_code, source_program_id, title, organization, summary,
            categories, regions, target_description, application_period_raw,
            application_start_date, application_end_date, source_url,
            source_sort_timestamp, startup_details, is_source_present
        ) VALUES
        <foreach collection="rows" item="row" separator=",">
            (
                #{row.sourceCode}, #{row.sourceProgramId}, #{row.title},
                #{row.organization}, #{row.summary},
                #{row.categoriesJson,jdbcType=VARCHAR}, #{row.regionsJson,jdbcType=VARCHAR},
                #{row.targetDescription}, #{row.applicationPeriodRaw},
                #{row.applicationStartDate,jdbcType=DATE}, #{row.applicationEndDate,jdbcType=DATE},
                #{row.sourceUrl}, #{row.sourceSortTimestamp,jdbcType=VARCHAR},
                #{row.startupDetailsJson,jdbcType=VARCHAR}, TRUE
            )
        </foreach>
        AS incoming
        ON DUPLICATE KEY UPDATE
            source_program_id = incoming.source_program_id,
            title = incoming.title,
            organization = incoming.organization,
            summary = incoming.summary,
            categories = incoming.categories,
            regions = incoming.regions,
            target_description = incoming.target_description,
            application_period_raw = incoming.application_period_raw,
            application_start_date = incoming.application_start_date,
            application_end_date = incoming.application_end_date,
            source_url = incoming.source_url,
            source_sort_timestamp = incoming.source_sort_timestamp,
            startup_details = incoming.startup_details,
            is_source_present = TRUE,
            last_seen_at = CURRENT_TIMESTAMP(6)
    </insert>

    <update id="markAllNotPresentBySourceCode">
        UPDATE support_program
        SET is_source_present = FALSE
        WHERE source_code = #{sourceCode}
          AND is_source_present = TRUE
    </update>

    <select id="findBySourceAndProgramId" resultMap="supportProgramDbRow">
        SELECT
            source_code,
            source_program_id,
            title,
            organization,
            summary,
            categories,
            regions,
            target_description,
            application_period_raw,
            application_start_date,
            application_end_date,
            source_url,
            source_sort_timestamp,
            startup_details
        FROM support_program
        WHERE source_code = #{sourceCode}
          AND source_program_id = #{sourceProgramId}
          AND is_source_present = TRUE
    </select>

    <select id="findPresent" resultMap="supportProgramDbRow">
        SELECT
            source_code,
            source_program_id,
            title,
            organization,
            summary,
            categories,
            regions,
            target_description,
            application_period_raw,
            application_start_date,
            application_end_date,
            source_url,
            source_sort_timestamp,
            startup_details
        FROM support_program
        WHERE is_source_present = TRUE
        ORDER BY source_code ASC, source_program_id ASC
    </select>

    <select id="findPublishedPresent" resultMap="supportProgramDbRow">
        SELECT
            program.source_code,
            program.source_program_id,
            program.title,
            program.organization,
            program.summary,
            program.categories,
            program.regions,
            program.target_description,
            program.application_period_raw,
            program.application_start_date,
            program.application_end_date,
            program.source_url,
            program.source_sort_timestamp,
            program.startup_details
        FROM support_program program
        INNER JOIN support_program_sync_status sync_status
            ON sync_status.source_code = program.source_code
        WHERE program.is_source_present = TRUE
          AND sync_status.published_generation IS NOT NULL
          AND sync_status.published_catalog_fingerprint IS NOT NULL
        <if test="requireIndexReady">
          AND sync_status.index_ready = TRUE
        </if>
        ORDER BY program.source_code ASC, program.source_program_id ASC
    </select>

    <insert id="upsertSourceDocument"
            parameterType="ai.govbiz.core.supportprogram.repository.mapper.SupportProgramSourceDocumentDbRow">
        INSERT INTO support_program_source_document (
            source_code,
            source_program_id,
            source_url,
            content,
            content_hash,
            fetched_at
        ) VALUES (
            #{sourceCode},
            #{sourceProgramId},
            #{sourceUrl},
            #{content},
            #{contentHash},
            #{fetchedAt}
        )
        ON DUPLICATE KEY UPDATE
            source_url = #{sourceUrl},
            content = #{content},
            content_hash = #{contentHash},
            fetched_at = #{fetchedAt}
    </insert>

    <select id="findPresentSourceDocument" resultMap="supportProgramSourceDocumentDbRow">
        SELECT
            document.source_code,
            document.source_program_id,
            document.source_url,
            document.content,
            document.content_hash,
            document.fetched_at
        FROM support_program_source_document document
        INNER JOIN support_program program
            ON program.source_code = document.source_code
            AND program.source_program_id = document.source_program_id
        WHERE document.source_code = #{sourceCode}
          AND document.source_program_id = #{sourceProgramId}
          AND program.is_source_present = TRUE
    </select>

    <insert id="insertSyncGenerationIfAbsent">
        INSERT IGNORE INTO support_program_sync_generation (source_code)
        VALUES (#{sourceCode})
    </insert>

    <select id="lockLatestStartedGeneration" resultType="long">
        SELECT latest_started_generation
        FROM support_program_sync_generation
        WHERE source_code = #{sourceCode}
        FOR UPDATE
    </select>

    <select id="findLatestStartedGeneration" resultType="long">
        SELECT latest_started_generation
        FROM support_program_sync_generation
        WHERE source_code = #{sourceCode}
    </select>

    <update id="updateLatestStartedGeneration">
        UPDATE support_program_sync_generation
        SET latest_started_generation = #{generation}
        WHERE source_code = #{sourceCode}
    </update>

    <select id="findSyncStatus" resultMap="supportProgramSyncStatusDbRow">
        SELECT
            source_code,
            published_generation,
            published_catalog_fingerprint,
            published_program_count,
            index_ready,
            last_successful_sync_at,
            last_failed_sync_at,
            last_sync_outcome
        FROM support_program_sync_status
        WHERE source_code = #{sourceCode}
    </select>

    <select id="findSyncStatuses" resultMap="supportProgramSyncStatusDbRow">
        SELECT
            source_code,
            published_generation,
            published_catalog_fingerprint,
            published_program_count,
            index_ready,
            last_successful_sync_at,
            last_failed_sync_at,
            last_sync_outcome
        FROM support_program_sync_status
        UNION ALL
        SELECT
            program.source_code AS source_code,
            NULL AS published_generation,
            NULL AS published_catalog_fingerprint,
            COUNT(*) AS published_program_count,
            FALSE AS index_ready,
            NULL AS last_successful_sync_at,
            NULL AS last_failed_sync_at,
            'NONE' AS last_sync_outcome
        FROM support_program program
        WHERE program.is_source_present = TRUE
          AND NOT EXISTS (
              SELECT 1
              FROM support_program_sync_status sync_status
              WHERE sync_status.source_code = program.source_code
          )
        GROUP BY program.source_code
        ORDER BY source_code ASC
    </select>

    <insert id="upsertSyncSuccess">
        INSERT INTO support_program_sync_status (
            source_code,
            published_generation,
            published_catalog_fingerprint,
            published_program_count,
            index_ready,
            last_successful_sync_at,
            last_sync_outcome
        ) VALUES (
            #{sourceCode},
            #{generation},
            #{catalogFingerprint},
            #{programCount},
            TRUE,
            #{occurredAt},
            'SUCCESS'
        )
        ON DUPLICATE KEY UPDATE
            published_generation = #{generation},
            published_catalog_fingerprint = #{catalogFingerprint},
            published_program_count = #{programCount},
            index_ready = TRUE,
            last_successful_sync_at = #{occurredAt},
            last_sync_outcome = 'SUCCESS'
    </insert>

    <insert id="upsertSyncFailure">
        INSERT INTO support_program_sync_status (
            source_code,
            index_ready,
            last_failed_sync_at,
            last_sync_outcome
        ) VALUES (
            #{sourceCode},
            FALSE,
            #{occurredAt},
            'FAILURE'
        )
        ON DUPLICATE KEY UPDATE
            last_failed_sync_at = #{occurredAt},
            last_sync_outcome = 'FAILURE'
    </insert>

    <update id="markSyncIndexReady">
        UPDATE support_program_sync_status
        SET index_ready = TRUE
        WHERE source_code = #{sourceCode}
          AND published_generation = #{publishedGeneration}
          AND published_catalog_fingerprint = #{catalogFingerprint}
          AND published_program_count = #{programCount}
    </update>

    <update id="markSyncIndexNotReady">
        UPDATE support_program_sync_status
        SET index_ready = FALSE
        WHERE source_code = #{sourceCode}
          AND published_generation = #{publishedGeneration}
          AND published_catalog_fingerprint = #{catalogFingerprint}
          AND published_program_count = #{programCount}
    </update>

    <insert id="insertSyncStatusIfAbsent">
        INSERT IGNORE INTO support_program_sync_status (source_code)
        VALUES (#{sourceCode})
    </insert>

    <update id="bootstrapSyncStatusIfUntrusted">
        UPDATE support_program_sync_status
        SET published_generation = 0,
            published_catalog_fingerprint = #{catalogFingerprint},
            published_program_count = #{programCount},
            index_ready = TRUE
        WHERE source_code = #{sourceCode}
          AND published_catalog_fingerprint IS NULL
    </update>
</mapper>
